
 <div class="container">
  <div class="row product-width" >
    <div class="col-md-12 " >
      <div class="filter">
        <ul>
          <li><a href="/"> 首页</a> </li>

        </ul>
      </div>
      <hr>
    </div>

<!-- grid商品栏目 -->
 <section id="shop-without-sidebar" style="margin: 20px auto 100px;">
  <div class="container">
    <div class="row">
      <% @products.each do |product| %>
      <div class="col-md-3 col-sm-6 col-xs-6">
        <div class="product-style-1 text-center">
          <%= link_to product_path(product) do %>
            <% if product.image.present? %>
            <%= image_tag(product.image.thumb.url, class: "img-responsive", alt: product.title) %>
            <% else %>
            <%= image_tag("http://placehold.it/200x200&text=No Pic", class: "img-responsive", alt: "None") %>
          <% end %>
        <% end %>
          <div class="product-detail-content">
            <h6><%= product.title %><h6>
              <div class="product-rating-with-border">
                <div class="background-border"></div>
              </div>
              <h5>￥<%= product.price %></h5>

              <!-- <a class="btn cart-button" href="#"> 加入购物车 </a> -->
               <% if product.quantity.present? && product.quantity > 0 %>
                <%= link_to("加入购物车", add_to_cart_product_path(product), method: :post, class: "btn cart-button") %>
              <% else %>
                已销售一空，无法购买！
              <% end %>
            </div>
          </div>
        </div>
        <% end %>

      </div>
    </div>
  </section>



  <!-- <div class="row">
    <% @products.each do |product| %>
      <div class="col-xs-6 col-md-3">
        <%= link_to product_path(product) do %>
          <% if product.image.present? %>
            <%= image_tag(product.image.thumb.url, class: "thumbnail") %>
          <% else %>
            <%= image_tag("http://placehold.it/200x200&text=No Pic", class: "thumbnail") %>
          <% end %>
        <% end %>
        <%= product.title %> ￥ <%= product.price %>
      </div>
    <% end %>
  </div> -->
